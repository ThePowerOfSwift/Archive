#include "t.js"function Environment(measureList) {        this.music = music;    this.getEnsembleInfo();    this.createViews();}Environment.prototype.getEnsembleInfo = function() {    // do this later :/};Environment.prototype.createViews = function() {    this.views = {};    for (var performer = 0; performer < this.performers.length; performer ++) {        var curPerformer = this.performers[performer];        // Soon, pass individual "format" variable, disregard for now        this.views[curPerformer] = new View(this, curPerformer);    };    // Add also "omni"};Environment.prototype.getPerformerOrderByID = function(primaryPerformerID) {    var performerOrderByPrimaryPerformerID = {        "FL": ["VA", "CL", "FL"],        "CL": ["VA", "FL", "CL"],        "VA": ["FL", "CL", "VA"],        "omni": ["FL", "VA", "CL"]    };    return performerOrderByPrimaryPerformerID[primaryPerformerID];};