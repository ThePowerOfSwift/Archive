function TimeSignature(Measure, x, top, type) {        this.Measure = Measure;    this.x = x;    this.top = top;    this.beats = this.Measure.beats;    this.subdivision = this.Measure.subdivision;    this.type = type || "ord"; // types come in as "ord", "courtesy"    this.textFont = app.textFonts.getByName("Baskerville-SemiBold");    this.textSize = 15; // make variable: System.g?    this.textHeight = 0.75 * this.textSize;    this.height = 2 * this.textHeight;}TimeSignature.prototype.draw = function(graphicalContext) {    this.graphicalContext = graphicalContext.groupItems.add();    this.textColor = brown_med;        this.beatsText();    this.subdivisionText();    //this[this.type](); // change opacity as appropriate for type    //this.graphicalContext.opacity = this.opacity;};TimeSignature.prototype.beatsText = function() {    var beats = this.graphicalContext.textFrames.add();    beats.contents = this.beats;    beats.top = this.top;    beats.left = this.x;    beats.textRange.characterAttributes.textFont = this.textFont;    beats.textRange.characterAttributes.size = this.textSize;    beats.textRange.characterAttributes.fillColor = this.textColor;    beats.textRange.paragraphAttributes.justification = Justification.CENTER;    beats.textRange.characterAttributes.tracking = -50;};TimeSignature.prototype.subdivisionText = function() {    var subdivision = this.graphicalContext.textFrames.add();    subdivision.contents = this.subdivision;    subdivision.top = this.top - this.textHeight;    subdivision.left = this.x;    subdivision.textRange.characterAttributes.textFont = this.textFont;    subdivision.textRange.characterAttributes.size = this.textSize;    subdivision.textRange.characterAttributes.fillColor = this.textColor;    subdivision.textRange.paragraphAttributes.justification = Justification.CENTER;    subdivision.textRange.characterAttributes.tracking = -50;};TimeSignature.prototype.ord = function() { this.opacity = 100; }TimeSignature.prototype.courtesy = function() { this.opacity = 50; }