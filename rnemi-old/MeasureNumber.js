function MeasureNumber(Measure, x, top, number) {        this.Measure = Measure;    this.x = x;    this.top = top;    this.number = number;    this.height = this.Measure.System.measureNumberHeight;};MeasureNumber.prototype.draw = function(graphicalContext) {        this.graphicalContext = graphicalContext.groupItems.add();    this.enclosure();    this.text();}MeasureNumber.prototype.enclosure = function() {    var textScale = 5;    var amountChar = this.number.toString().length;    var pad = 7;    var width = textScale * amountChar + pad;     var height = this.height;    var enclosure = this.graphicalContext.pathItems.rectangle(        this.top,        this.x - 0.5 * width,        width,        this.height    );    enclosure.strokeWidth = 0.25;    enclosure.strokeColor = gray[50];}MeasureNumber.prototype.text = function() {    // Draw number    var textSize = 9.5;    var textFont = app.textFonts.getByName("AvenirNextCondensed-Medium");    var number = this.graphicalContext.textFrames.add();    number.contents = this.number;    number.top = this.top + 1.618;    number.left = this.x;    number.textRange.characterAttributes.textFont = textFont;    number.textRange.characterAttributes.size = textSize;    number.textRange.characterAttributes.fillColor = black;    number.textRange.paragraphAttributes.justification = Justification.CENTER;}