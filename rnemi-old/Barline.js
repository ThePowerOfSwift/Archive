function Barline(graphicalContext, x, top, bottom, type) {        this.x = x;    this.top = top;    this.bottom = bottom;    this.type = type || "ord";        this.strokeWidth = 3;    this.strokeColor = this.strokeColorByType(this.type);    this.opacity = 100;        var barline = graphicalContext.pathItems.add();    barline.setEntirePath([        [this.x, this.top],        [this.x, this.bottom]    ]);    barline.strokeWidth = this.strokeWidth;    barline.strokeColor = this.strokeColor;    barline.filled = false;    barline.opacity = this.opacity    barline.locked = false;}Barline.prototype.strokeColorByType = function(type) {    var strokeColorByType = {        "ord": gray[10],        "systemEnd": gray[16],        "pieceEnd": gray[24]    };    return strokeColorByType[type];}