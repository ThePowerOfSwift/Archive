#include "../testDoc.js"#include "RhythmTest.js"#include "../Staff.js"#include "../NormalSequence.js"curScore = {};curScore.beat_width = 72;x = 50;//var staff = new Staff(35, 400, 5, "treble", 0);for (var i = 0; i < 5; i ++) {        var beats = Math.floor(Math.random()*7 + 4);    var subdivision = 16;    var amount = Math.floor(Math.random()*7 + 3);    //var expList = [3,4,6,8,12,16,24,32];    var durList = new NormalSequence(        null,        null    ).wholeSeq("byAmount", amount);    var rhythm = new RhythmTest(x, 1, beats, subdivision, durList);    x += beats / subdivision * 8 * curScore.beat_width;};