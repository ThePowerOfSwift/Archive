#include "../testDoc.js"#include "../colors.js"#include "../arrayMethods.js"#include "../FING_FL_C.js"#include "../FING_CL_Bflat.js"#include "../FluteDiagram.js"#include "../ClarinetDiagram.js"#include "../Staff.js"var x = 50;var staff = new Staff(x - 15, 350, 5, "treble", 0);for (var f = 0; f < FING_FL_C.length; f ++) {    var fingering = FING_FL_C[f];        if (fingering.pitch.sounding.contains(85)/* &&        fingering.pitch.sounding.contains(85)*/ &&        fingering.difficulty === "easy"    ) {         t.w("FLUTE: " + fingering.pitch.sounding);                // draw fingering digram        var fingeringDiagram = new FluteDiagram(x, 400, 5, fingering.fingering, "concert");        var pitchINFO = {};        for (var p = 0; p < fingering.pitch.sounding.length; p ++) {            var pitch = fingering.pitch.sounding[p];            pitchINFO[pitch] = {};            pitchINFO[pitch].type = "ord";            pitchINFO[pitch].color = gray[50];        };        staff.pitch(x, pitchINFO);                x += 36;    };};var x = 50;var staff = new Staff(x - 15, 150, 5, "treble", 0);for (var f = 0; f < FING_CL_Bflat.length; f ++) {    var fingering = FING_CL_Bflat[f];    if (fingering.pitch.sounding.contains(85)/* &&        fingering.pitch.sounding.contains(85)*/ &&        fingering.difficulty === "easy"    ) {        t.w("CLARINET: " + fingering.pitch.sounding)                // draw fingering diagram        var fingeringDiagram = new ClarinetDiagram(x, 200, 5, fingering.fingering, "Bflat");        var pitchINFO = {};        for (var p = 0; p < fingering.pitch.sounding.length; p ++) {            var pitch = fingering.pitch.sounding[p];            pitchINFO[pitch] = {};            pitchINFO[pitch].type = "ord";            pitchINFO[pitch].color = gray[50];        };        staff.pitch(x, pitchINFO);                x += 36;    };    };