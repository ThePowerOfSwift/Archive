#include "../t.js"#include "../NormalSequence.js"//#include "MultiDepthRhythm.js"for (var i = 0; i < 20; i ++) {    var seqModel = new NormalSequence();    var seq = seqModel.wholeSeq("byAmount", 5);    t.w("seq.sequence: " + seq + "; amountDirSpans: " + seqModel.dirSpans.length);        for (var d = 0; d < seqModel.dirSpans.length; d ++) {        var dirSpan = seqModel.dirSpans[d];        t.w(dirSpan.indices + ": dir: " + dirSpan.direction +             ": seq: " + dirSpan.sequence        );    };};//t.w("dirSpans: " + seqModel.dirSpans);//var mdr = new MultiDepthRhythm().relative();//t.w(mdr + ": " + mdr.sum());/*var seqModel = new NormalSequence();var seq = seqModel.wholeSeq("byAmount", 9);seqModel.selfAnalyze();t.w(seq);t.w("maxDelta: " + seqModel.greatestDelta + "; at Index: " + seqModel.greatestDeltaIndex);for (var d = 0; d < seqModel.dirSpans.length; d ++) {    var dirSpan = seqModel.dirSpans[d];    t.w("dirSpan ======================");    for (var el in dirSpan) {           t.w(el + ": " + dirSpan[el]);    };};*/