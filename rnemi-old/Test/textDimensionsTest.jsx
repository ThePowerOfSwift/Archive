#include "../testDoc.js"function MeasureNumber(x, baseline, num) {        this.x = x || 400;    this.baseline = baseline || 400;    this.num = num || 42;        this.enclosure_height = 12; // make relative!        // Calculate enclosure width based on length of measure number    this.enclosure_width = 5*this.num.toString().length + 7; // do actual calculation!    this.enclosure_color = gray[50];        var measureNumGroup = testDoc.groupItems.add();    // Draw enclosure    var enclosure = measureNumGroup.pathItems.rectangle(                this.baseline + this.enclosure_height,        this.x - .5*this.enclosure_width,        this.enclosure_width,        this.enclosure_height    );    enclosure.strokeWidth = .25;    enclosure.strokeColor = this.enclosure_color;        this.textSize = 12;    this.textFont = app.textFonts.getByName("AvenirNextCondensed-Medium");    this.textColor = black;        // Draw number    var number = measureNumGroup.textFrames.add();    number.contents = this.num;    number.top = this.baseline + 11.75;    number.left = this.x;    number.textRange.characterAttributes.textFont = this.textFont;    number.textRange.characterAttributes.size = this.textSize;    number.textRange.characterAttributes.fillColor = this.textColor;    number.textRange.paragraphAttributes.justification = Justification.CENTER;    var outlinedNumber = number;    var outlines = outlinedNumber.createOutline();    outlines.remove();        };var m = new MeasureNumber();