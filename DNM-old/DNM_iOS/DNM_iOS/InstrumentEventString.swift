//
//  InstrumentEventString.swift
//  DNM
//
//  Created by James Bean on 1/14/16.
//  Copyright © 2016 James Bean. All rights reserved.
//

import DNMModel

// TODO: implement
public class InstrumentEventString: InstrumentEvent {
    
    /*
    // TODO: extend this as String instrument representation extends
    internal override func createGraphEventfor(component: Component) {
        guard canRenderComponent(component) else { return }
        let graphEvent = GraphEvent.with(.Staff,
            identifier: "staff",
            leaf: leaf,
            x: x,
            width: width,
            stemDirection: stemDirection
        )
    }
    
    // TODO: extend this as String instrument representation extends
    internal override func identifierFor(component: Component) -> GraphID? {
        switch component {
        case is ComponentNode: return "graph"
        case is ComponentEdge: return "graph"
        case is ComponentPitch: return "staff"
        default: return nil
        }
    }
    */
}